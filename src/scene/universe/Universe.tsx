'use client';

import { DragonUniverse } from '@/scene/dragon/DragonUniverse';

/** Home universe — dragon command mock. Industrial plaza is not live. */
export function Universe() {
  return <DragonUniverse />;
}
