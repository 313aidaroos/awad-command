# Build plan

The step-by-step plan is **Parts A–D** of `docs/MASTER_HANDOFF.md`.

Tonight’s working slice: live Fleet panel (`GET /api/fleet`) probes public company URLs and shows up/down on the universe HUD. Metrics stay DEMO until live sources connect.
