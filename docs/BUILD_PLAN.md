# Build plan

The step-by-step plan is **Parts A–D** of `docs/MASTER_HANDOFF.md`.

Tonight’s working slice: boot, universe orbs (including Nursery Toons and Qahwahworld stubs), ProjectWorld HUD with Message lead, `/api/ceo` demo responder, `/api/lead-message` webhook stub, record-only approvals, Computer placeholder, Supabase auth gate that stays open until URL + anon + `ALLOWED_EMAIL` are all set.
